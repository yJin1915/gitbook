import * as React from 'react';

export interface ILoginProps {
  defaultActiveKey?: string;
  style?: React.CSSProperties;
}

export default class UserLogin extends React.Component<ILoginProps, any> {
}
